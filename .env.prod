ENV=prod

# Application Configuration
APP_PORT=8080

# Database Configuration (External Server)
# CRITICAL: Define the hostname that Docker will map to the host IP
DB_HOST=hostmachine

# CRITICAL: Use the DB_HOST environment variable in the URL
DB_URL=jdbc:mysql://${DB_HOST}:3306/backendall?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
DB_USERNAME=lorion
DB_PASSWORD=lorion@2025!
HOST_MACHINE_IP=192.168.1.15 # Confirmed correct IP

# JWT Configuration
JWT_SECRET=tR1CSQJsTTNpq9oONu4nVIJIZYLUSlpMoCvtXCLqQS76ZZU9DWdglhBSizsIRR2
JWT_EXPIRATION=86400000
